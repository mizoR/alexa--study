{
  "name": "alexa-study",
  "version": "1.0.0",
  "main": "handler.js",
  "author": "mizokami <r.mizokami@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p pkg && rm -rf pkg/* handler.js && tsc && zip pkg/archive.zip -r *"
  },
  "dependencies": {
    "@types/alexa-sdk": "^1.1.1",
    "@types/node": "^9.3.0",
    "alexa-sdk": "^1.0.25"
  }
}
